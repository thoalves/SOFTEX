//Crie um programa que contenha os seguintes tipos de funções:
//1. uma função tradicional com a palavra reservada “Function” e sem nenhum parâmetro;
//2. uma função tradicional com parâmetros e um retorno de valor;
//3. uma arrow function com parâmetros e que retorne um valor.

//Crie um programa que utilize essas três funções de forma lógica, por exemplo: um programa de calculadora.
//Trabalhe esse código em seu IDE, suba ele para sua conta no GitHub e compartilhe o link desse projeto no campo ao lado para que outros desenvolvedores possam analisá-lo.

let nome = prompt("Insira seu nome: ")
let anoNascimento = parseFloat(prompt("Insira o ano do nascimento: "))

function abertura(){
	console.log("Olá. Seja bem vindo ao cadastro.")
}

abertura();

function exibirNome(nome) {
	return nome
}

console.log(exibirNome(nome));


let idade = (ano) => {return(2022-ano)}

console.log(idade(anoNascimento))
