//Crie dois códigos de sistema de notas para uma escola. O primeiro código deve ser um programa que informa se o aluno reprovou, ou não, com base nas três notas 
//que ele adicionou ao programa. Utilize, no mínimo, um operador de atribuição e um operador ternário. [ATIVIDADE A]
//O segundo código é um programa que o aluno deve escrever duas notas e o retorno informa a nota mínima que ele deve tirar na próxima prova para poder passar com 
//nota sete.

var nota_1, nota_2, nota_3, resultado

nota_1 = parseFloat(prompt('Nota 1: '))
nota_2 = parseFloat(prompt('Nota 2: '))
nota_3 = parseFloat(prompt('Nota 3: '))

resultado = ((nota_1+nota_2+nota_3)/3 >= 7) ? "Aprovado" : "Reprovado"

console.log(resultado)
